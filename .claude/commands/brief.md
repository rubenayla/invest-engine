# brief — Portfolio intelligence briefing

Scan portfolio for sell signals, rank watchlist for buy opportunities, present an actionable brief. Checks data freshness, updates if stale (>24h).

See `.claude/skills/brief/SKILL.md` for the full protocol.

Pass through any arguments: `--skip-update`, `--sells-only`, `--buys-only`.
