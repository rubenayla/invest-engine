# Tasks

## Re-run insider snapshot backfill against the investment database

- [ ] Run `uv run python scripts/backfill_insider_snapshots.py` and `uv run python scripts/dashboard.py` after the configured `invest` PostgreSQL database is restored or the connection file points to the correct database.
