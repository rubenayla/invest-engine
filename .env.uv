# UV Configuration for Local Caching (CI/CD best practices)
# Source this file before running uv commands in CI/CD: source .env.uv

# Use project-local cache directory instead of global cache
export UV_CACHE_DIR="${PWD}/.uv/cache"

# Link cache for faster repeated installs
export UV_LINK_MODE="copy"

# Show progress for CI/CD visibility
export UV_PROGRESS="plain"

# Usage in CI/CD:
# 1. source .env.uv
# 2. uv sync
# 3. uv run pytest
